#include <vector>
#include <set>

// concepts introduced in c++20; evaluated at compile time

template<typename T>
void add(T elem)

int main() {
    std::vector<int> v;
    std::set<int> s;
    return 0;
}
